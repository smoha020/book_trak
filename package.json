{
  "name": "Root",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "12.16.0"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \" cd backend && npm run server  \" \" cd front_end && npm run start  \"", 
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix front_end && npm run build --prefix front_end"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
